<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Application layer: use-case orchestration. Defines the ports (interfaces the outer layers
    implement), the CQRS messages (ICommand<T>/IQuery<T>), their handlers, pipeline behaviors,
    and DTOs. CQRS is wired with the source-generated Mediator (martinothamar); validation runs
    as a pipeline behavior backed by FluentValidation. Depends only on Domain.
  -->
  <ItemGroup>
    <PackageReference Include="Mediator.Abstractions" />
    <PackageReference Include="Mediator.SourceGenerator" PrivateAssets="all" OutputItemType="Analyzer" ReferenceOutputAssembly="false" />
    <!-- CommunityToolkit.Mvvm is UI-framework-agnostic; used here only for its Messaging primitives
         (IMessenger / WeakReferenceMessenger) that back the instant-apply settings channel (WHISPER-78). -->
    <PackageReference Include="CommunityToolkit.Mvvm" />
    <PackageReference Include="FluentValidation" />
    <PackageReference Include="FluentValidation.DependencyInjectionExtensions" />
    <PackageReference Include="Riok.Mapperly" />
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" />
    <PackageReference Include="Microsoft.Extensions.Configuration.Abstractions" />
    <PackageReference Include="Microsoft.Extensions.Options.ConfigurationExtensions" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Domain\Domain.csproj" />
  </ItemGroup>

</Project>
