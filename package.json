{
  "name": "whisper-net-tooling",
  "version": "0.0.0",
  "private": true,
  "description": "Developer tooling for whisper.net: Conventional Commits enforcement (commitlint) wired as a Git commit-msg hook via husky. Not part of the .NET application.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.0",
    "@commitlint/config-conventional": "^21.0.0",
    "husky": "^9.1.0"
  }
}
